# Java

## 0 Java入门

### 0.1 简单CMD命令

|操作|说明|
|:---:|:---:|
|盘符名+冒号|盘符切换|
|dir|查看当前路径下所有文件|
|cd 目录名|进入单级目录|
|cd 目录1\目录2\\...|进入多级目录|
|cd ..|回到上级文件夹|
|cd \\ |回退到盘符目录|
|cls|清屏|
|exit|退出|

### 0.2 跨平台原理--虚拟机JVM

Java为混合型语言

.java -> .class（虚拟机中运行）

### 0.3 JRE与JDK

JDK：Java开发工具包

- JVM：Java虚拟机
- 核心类库
- 开发工具

JRE：Java运行环境（部分JDK）

( JDK ( JRE ( JVM ) ) )

## 1 Java基础概念

### 1.1 注释

|类型|方法|
|---|---|
|单行注释|//|
|多行注释|/**/|
|文档注释|/***/|

### 1.2 关键字-特定含义的英文单词

关键字字母全小写

#### 1.2.1 class

用于（创建/定义）一个类。类是Java最基本的组成单元。

### 1.3 字面量

|字面量类型|说明|举例|
|:---|:---|:---|
|整数类型|不带小数点的数字|1，2|
|小数类型|带小数点的数字|1.1，2.2|
|字符串类型|双引号括起来的内容|"你好"，""|
|字符类型|单引号括起来的内容，只能有一个|'1'|
|布尔类型|布尔值，表示真假|true,false|
|空类型|特殊值，空值|null|

#### 1.3.1 特殊扩展

\t 制表符 补空格，根据前面补到8或8的倍数，最少一个，最多8个

### 1.4变量

#### 1.4.1 定义格式

数据类型 变量名 = 数据值;

#### 1.4.2 变量用法

1. 基本用法
1. 变量参与计算
1. 修改变量记录值

#### 1.4.3 注意事项

一条语句定义多个变量。

```java
int a = 1, b = 2, c = 3;
```

变量使用前必须赋值。

### 1.5 计算机存储规则

- 文本
- 图片
- 声音

任意数据都是以二进制存储数据的。

- 二进制0b
- 八进制0
- 十六进制0x

转换：小数点前除基倒取余法。小数点后乘基减基法。

#### 1.5.1 文本

1. GB2312编码：中国简体
1. BIG5：台湾繁体
1. GBK：包含GB3000-1全部中日韩和BIG5全部
1. Unicode：国际标准字符集

#### 1.5.2 图片

分辨率
像素
三原色：光学（红绿蓝）

黑白图
灰度图：纯黑0-255纯白
色彩图

#### 1.5.3 声音

对声音的波形图进行采样再存储

### 1.6 数据类型

#### 1.6.1 基本数据类型

整数

- byte：1
- short：2
- int(默认)：4
- long：8(加L/l后缀)

浮点数

- float：4(加F/f后缀)
- double(默认)：8

字符：char

布尔：boolean

#### 1.6.2 引用数据类型

### 1.7 标识符

#### 1.7.1 硬性

数字，字母，下划线，美元符$

数字不开头
大小写严格
不能是关键字

#### 1.7.2 软性

小驼峰命名法（方法变量）：标识符一个单词，全部小写：多个单词，第一个单词首字母小写，其他单词首字母大写。

大驼峰命名法（类名）：每个单词首字母都大写

### 1.8 键盘录入

Scanner类，可以接受键盘输入的数字

1. 导包（找）
    `import java.util.Sacnner;`在类定义前
1. 创建对象（准备）

    `Scanner sc = new Scannner(System.in);`sc可以改
1. 接收数据（开始）
    `int i = sc.nextInt();`i可以改

### 1.9 IDEA

intelliJ IDEA